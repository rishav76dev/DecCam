{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run index.ts",
    "watch": "bun --watch index.ts",
    "scrape-test": "bun run scraper.ts"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
